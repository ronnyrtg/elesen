@model TradingLicense.Model.BusinessTypeModel
@using AutoMapper;
@{
    ViewBag.Title = "ManageBusinessType";
}
<div id="divMessage"></div>
<div class="card">
    <div class="header">
        <h4 id="headerTitle">Manage Business Type</h4>
    </div>
    <div class="body">

        @using (Html.BeginForm(null, null, FormMethod.Post, new { name = "frmBusinessType", id = "frmBusinessType" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.BT_ID)
            <div class="row">
                <div class="col-sm-12">
                    <div class="form-group">
                        @Html.LabelFor(m => m.BT_CODE, new { @class = "control-label" }) <span class="cfont">*</span>
                        @Html.TextBoxFor(m => m.BT_CODE, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.BT_CODE)
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <div class="form-group">
                        @Html.LabelFor(m => m.BT_DESC, new { @class = "control-label" }) <span class="cfont">*</span>
                        @Html.TextBoxFor(m => m.BT_DESC, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.BT_DESC)
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <div class="form-group">
                        <div class="checkbox">
                            <label>
                                @Html.CheckBoxFor(model => model.ACTIVE)
                                <span>Aktif?</span><span class="pointer"><img src="~/images/checkmark.svg" alt="Checkmark" /></span>
                            </label>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row" style="max-height:180px; overflow:auto;">
                <div class="col-sm-12">
                    <div class="form-group">
                        <div class="control-label">Required Documents.</div>
                        @foreach (var item in ViewBag.AllRequiredDocs)
                        {
                            <div class="checkbox">
                                <label>
                                    <input type="checkbox"
                                            name="RequiredDocs"
                                            value="@item.RD_ID"
                                            @if(Model.RequiredDocs?.Any(rdID => rdID == item.RD_ID) == true){ <text>checked</text> }
                                            />
                                    <span>@item.RD_DESC</span><span class="pointer"><img src="~/images/checkmark.svg" alt="Checkmark" /></span>
                                </label>
                            </div>
                        }
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <div class="form-group">
                        <input type="submit" class="btn btn-primary" value="Save" />
                        <input type="button" class="btn btn-default" value="Cancel" onclick="back()" />
                    </div>
                </div>
            </div>
        }
    </div>

                </div>
                @section scripts{
                    <script>
                        var actionError = '@(TempData["ErrorMessage"])';
                        if (actionError != null && actionError != "") {
                            errorMessage(actionError);
                        }
                        function back() {
                            var link = '@Url.Action("BusinessType", "Master")';
                            window.location.href = link;
                        }
                    </script>
                }
