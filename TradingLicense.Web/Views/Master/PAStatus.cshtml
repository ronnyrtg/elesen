
@{
    ViewBag.Title = "PAStatus";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="divMessage"></div>
<div class="card">
    <ul class="page-action-button">
        <li><a href="#" title="Add PAStatus" onclick="addPAStatus();"><img src="@Url.Content("~/images/add.png")" alt="Add PAStatus"></a></li>
    </ul>
    <div class="header">
        <h4>PA Status</h4>
    </div>
    <div class="filter-block">
        <div class="row small-gutter">
            <form>
                <div class="col-xs-4">
                    <input type="text" name="txtstatusDesc" id="txtstatusDesc" class="form-control" placeholder="Status Description" maxlength="100" />
                </div>
                <div class="col-xs-4">
                    <button type="button" class="btn btn-primary" onclick="return bindTable();">Search</button>
                    <button type="button" class="btn btn-default" onclick="return clearControls();">Clear</button>
                </div>
            </form>
        </div>
    </div>
    <div class="grid" id="divtblPAStatus">
        <table cellspacing="0" class="table table-striped" id="tblPAStatus"></table>
    </div>
</div>

@section scripts{
    <script type="text/javascript">
        $(function () {
            $('.form-control').keypress(function (e) {
                if (e.which == 13) {
                    bindTable();
                    return false;
                }
            });
         var success = '@(TempData["SuccessMessage"])';
                if (success != null && success != "") {
                    successMessage(success);
                }
        bindTable();
        });

        function addPAStatus() {
           var link = '@Url.Action("ManagePAStatus", "Master")';
             window.location.href = link;
        }

     function bindTable() {
         if (!isValidSearchCriteria('SearchCriteria')) { return false; }
         if ($.fn.DataTable.isDataTable("#tblPAStatus")) {
             $('#tblPAStatus').dataTable().fnDestroy();
             $('#divtblPAStatus').html('<table class="table table-striped" width="100%"  id="tblPAStatus"></table>');
          }

         table = $("#tblPAStatus").dataTable({
             "autoWidth": false,
             "searching": false,
             "paging": true,
             "sPaginationType": "full_numbers",
             "language": {
                 "lengthMenu": "Page Size : _MENU_ ",
                 "info": "Records _START_ to _END_ of (_TOTAL_)",
                 "emptyTable": "No Record(s) Found"
             },
             "processing": true,
             "serverSide": true,
             "pageLength": 10,
                    "ajax": {
                        url: '@Url.Action("PAStatus", "Master")',
                        type: "POST",
                        data: bindSearch()
                    },
             "columns": [
                 { "title": "Status Description", "data": "StatusDesc" },
                 { "title": "Percent Progress", "data": "PercentProgress" },
                        {
                    "title": "Action",
                    "data": null,
                    "render": function (data, type, row) {
                        var appendstring = '';
                        appendstring += '<a  href="@Url.Action("ManagePAStatus", "Master")?Id=' + row["PAStatusID"] + '" title="Edit"> <img src="@Url.Content("~/images/edit.png")" alt="Edit" /></a>&nbsp; '
                        appendstring += '<a href="javascript:void(0);" data-id="' + row["PAStatusID"] + '" onclick="bindDelete(this);" title="Delete"><img src="@Url.Content("~/images/delete.png")" alt="Delete" /></a>';
                        return appendstring
                    },
                            "width": "80px",
                    "orderable": false,
                    "class": "action-icons"
                }

             ],
             "drawCallback": function (settings) {
                 //hide paging and info when empty datattable
                 var api = this.api();
                 if (api.rows({ page: 'current' }).data().length <= 0) {
                     $('#tblPAStatus_info').hide();
                     $('#tblPAStatus_paginate').hide();
                     $('#tblPAStatus_length').hide();
                 }
                 else {
                     $('#tblPAStatus_info').show();
                     $('#tblPAStatus_paginate').show();
                     $('#tblPAStatus_length').show();
                 }
                 bindSortingArrow();
             }
                });
        }

        function clearControls() {
            $("#txtstatusDesc").val('');
            bindTable();
        }

        function bindSearch() {
            var param = {};
            param["statusDesc"] = $('#txtstatusDesc').val();
            return param;
        }

        function isValidSearchCriteria(formName) {
            var isValid = true;

            $("form#" + formName + " input[type=text]").each(function () {
                if ($(this).val().match(/[<>]/)) {
                    $(this).addClass('input-validation-error');
                    isValid = false;
                }
                else {
                    $(this).removeClass('input-validation-error');
                }
            });
            return isValid;
        }

        function bindDelete(item) {
            var x = confirm("Are you sure you want to delete?");
            if (x) {
                $.ajax({
                    type: "POST",
                    url: encodeURI('@Url.Action("DeletePAStatus", "Master")'),
                    data: { "Id": $(item).attr('data-id') },
                    cache: false,
                    dataType: 'json',
                    success: function (data, textStatus, XMLHttpRequest) {
                        if (data.success) {
                            bindTable();
                            successMessage(data.message);

                        }
                        else {
                            successMessage(data.message);
                        }
                    },
                    error: function (jqXHR, exception, errMsg) {
                    }
                });
            }
        }

    </script>
}


